# E2E Audit Baseline — 2026-08-11

This is a read-only baseline captured before the current audit. It is not release evidence for a deployed Teams app.

## Identity

| Field | Observed value |
|---|---|
| Worktree | `/Users/doosansmacbookpro/Documents/TeamsApp` |
| Branch | `codex/teams-mobile-genui` |
| Current HEAD | `e6540cc02c361e71dd63b86832e0e753425ea70c` |
| `package.json` | `1.0.40` |
| `package-lock.json` | `1.0.40` |
| `appPackage/manifest.json` | `1.0.40` |
| Active release run | `729914e4-130f-478b-8b8d-f2087576d66b` |
| Recorded release commit | `0945321522a922bd5571db0a53de112f0d3b6e51` |
| Recorded release version | `1.0.39` |
| Recorded release status | `PUBLIC_READY` |
| Recorded evidence | `portal=null`, `installed=null`, `desktop=null`, `mobile=null` |

The active worktree is therefore not the same release as `.release/current.json`. The recorded public/package evidence must not be reused for the current source.

## Process baseline

| Process | PID | Observation |
|---|---:|---|
| Dev Tunnel host | 49618 | Existing public tunnel; preserve while auditing |
| `npm start` | 98665/98678 | Existing public runtime; preserve while auditing |
| Older `node scripts/start-server.mjs` | 84835 | Separate older local runtime; do not use as public evidence |

The simultaneous server processes are a `STALE_PROCESS_SUSPECTED` condition until ownership, port, and release identity are correlated.

## Source I/O baseline

The first capture showed normal materialization for `package.json`, `package-lock.json`, `src/server/index.ts`, `src/client/App.tsx`, and `scripts/release-loop.mjs`; `appPackage/manifest.json` was still observed as `blocks=0`, `compressed,dataless`. FileProvider state is therefore not stable enough to assume from one successful read. Each release run must record these flags immediately before source read, build, package, and upload.

## Required disposition

- Current release: `BLOCKED` by release identity drift and missing external evidence.
- Public runtime: existing, but it proves only the recorded `1.0.39` release until a same-identity probe is captured.
- Portal/installed/desktop/mobile: unverified.
- New browser tabs: not created.
